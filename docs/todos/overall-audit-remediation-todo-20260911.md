# 全项目复审整改 Todo（2026-09-11）

## 目标

消除本轮复审确认的停止边界竞态、探索轨迹不一致、幂等误判、敏感文本泄露和旧发布产物误用风险，并把每项整改固化为自动回归。

## Todo

- [x] R1 目标页用例授权、Runner 状态复核和 Replay 启动纳入同一执行锁；启动失败恢复一次性用例状态。
- [x] R2 Runner 文本授权、前台校验和 scrcpy 注入纳入 Stop 执行边界。
- [x] R3 目标页计划改为延迟提交；相同请求复用原 token，同键异参不覆盖活动计划。
- [x] R4 Runner 与智能探索保存规范化配置指纹；同键同参幂等，同键异参明确冲突。
- [x] R5 探索器以步骤索引跟踪待决转移，避免切片扩容后 Destination 写入旧副本。
- [x] R6 外部应用恢复预算改为连续失败计数，稳定返回目标包后重置。
- [x] R7 聚焦文本采集增加资源 ID、描述、输入类型和掩码文本的敏感字段防御。
- [x] R8 Companion、UiAutomator 和验证夹具的无签名构建主动移除同目录旧签名 APK。
- [x] R9 部署默认要求六产物发布清单，并逐项核验版本、大小和 SHA-256；开发部署必须显式选择 `AllowUnmanifestedArtifacts`。
- [x] R10 竞态脚本使用项目内缓存，避免受主机系统缓存权限影响。
- [x] T1 增加 Stop/目标页授权并发回归。
- [x] T2 增加 Stop/文本注入并发回归。
- [x] T3 增加同键异参 Runner/探索回归。
- [x] T4 增加普通动作跨权限页后的步骤/图一致性回归。
- [x] T5 增加同一外部包四次成功恢复回归。
- [x] T6 增加 `password=false` 但元数据敏感及掩码文本回归。
- [x] V1 全量 Go 单元测试通过。
- [x] V2 `go vet ./agent/...` 通过。
- [x] V3 HTTP、探索和 Runner 定向 Race 检测通过。
- [x] V4 PowerShell 修改脚本语法检查通过。
- [x] V5 无签名夹具构建验证旧签名文件被移除，生成 APK 为 `2.0-expanded`。
- [ ] D1 使用正式密钥重新生成 Companion、UiAutomator 两个 APK及六产物 `release-manifest.json`，再执行发布门禁。
- [ ] D2 建立 Git 首次可信基线；当前仓库仍为零提交、零跟踪文件，此操作由仓库所有者确认后执行。

## 当前交付边界

代码整改和自动回归已完成。D1 需要正式签名密钥，不能使用临时密钥伪造发布结果；D2 会改变仓库历史，因此不在未明确授权时自动提交。发布门禁在这两个条件满足前保持失败是预期的安全行为。
